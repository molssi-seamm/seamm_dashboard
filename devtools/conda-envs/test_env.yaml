name: test
channels:
  - defaults
dependencies:
  # Nodejs needed from anaconda, not pip, to have npm
  - nodejs
  - pip
  - pip:
    - flask-authorize
    - flask-jwt-extended
    - email_validator
    - configargparse
    - connexion[swagger-ui]
    - fasteners
    - flask
    - flask-bootstrap4
    - flask_cors
    - flask_debugtoolbar
    - flask_mail
    - flask_marshmallow
    - flask_moment
    - flask_sqlalchemy
    - flask_wtf
    - marshmallow
    - marshmallow_sqlalchemy
    - python_dateutil
    - requests
    - selenium
    - sqlalchemy<1.4
    - wtforms
    # For testing:
    - pytest
    - pytest-selenium
    - pytest-flask
    - pytest-cov
